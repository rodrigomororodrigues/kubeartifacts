apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: example-com
  namespace: default
spec:
  secretName: dashboard-tls
  issuerRef:
    name: letsencrypt-prod # Change based on your Issuer configuration
    kind: ClusterIssuer
  dnsNames:
  - domain.net
